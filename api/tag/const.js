/**
 * Constants and definitions for the tag database
 */

const columns = {
    id: {
        db: {
            type: "INTEGER",
            primaryKey: true
        },
        api: {
            readOnly: true
        }
    }
}
/**
 * Constants and definitions for the image database
 */

const columns = {
    id: {
        db: {
            type: "INTEGER",
            primaryKey: true
        },
        api: {
            readOnly: true
        }
    }
}